@{
    ViewData["Title"] = "Cashier home";
}

<div class="p-4 bg-light border rounded">
    <h1 class="h4 mb-1">Cashier home</h1>
    <p class="text-muted mb-0">Welcome, @User.Identity?.Name.</p>
</div>


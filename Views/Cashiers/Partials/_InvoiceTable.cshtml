@model IEnumerable<InvoiceListVm>

@if (!Model.Any())
{
    <div class="text-center text-muted py-4">
        <i class="fas fa-file-invoice fa-3x mb-3"></i>
        <div>Không tìm thấy hóa đơn nào.</div>
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table table-hover">
            <thead class="table-light">
                <tr>
                    <th>Mã hóa đơn</th>
                    <th>Biển số xe</th>
                    <th>Khách hàng</th>
                    <th>Xe</th>
                    <th>Trạng thái</th>
                    <th>Tổng tiền</th>
                    <th>Ngày tạo</th>
                    <th>Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var invoice in Model)
                {
                    <tr>
                        <td><strong>#@invoice.OrderId</strong></td>
                        <td>
                            <span class="badge bg-secondary">@invoice.LicensePlate</span>
                        </td>
                        <td>@invoice.CustomerName</td>
                        <td>@invoice.BikeModel</td>
                        <td>
                            @{
                                var statusClass = invoice.Status switch
                                {
                                    "pending" => "bg-warning",
                                    "quoted" => "bg-info",
                                    "in_progress" => "bg-primary",
                                    "completed" => "bg-success",
                                    "cancelled" => "bg-danger",
                                    _ => "bg-secondary"
                                };
                                var statusText = invoice.Status switch
                                {
                                    "pending" => "Chờ xử lý",
                                    "quoted" => "Đã báo giá",
                                    "in_progress" => "Đang xử lý",
                                    "completed" => "Hoàn thành",
                                    "cancelled" => "Đã hủy",
                                    _ => invoice.Status
                                };
                            }
                            <span class="badge @statusClass">@statusText</span>
                        </td>
                        <td><strong>@invoice.TotalPrice?.ToString("N0") VNĐ</strong></td>
                        <td>@invoice.CreatedAt?.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <button class="btn btn-outline-primary" data-invoice-action="details" data-id="@invoice.OrderId" title="Chi tiết">
                                    <i class="fas fa-eye"></i>
                                </button>
                                @if (invoice.Status == "pending" || invoice.Status == "quoted")
                                {
                                    <button class="btn btn-outline-success" data-invoice-action="quote" data-id="@invoice.OrderId" title="Tạo báo giá">
                                        <i class="fas fa-file-invoice-dollar"></i>
                                    </button>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
